class Solution {
public:
    vector<int> findAnagrams(string s, string p) {
        if (s.size() < p.size())
            return {};
        vector<int> ans;
        vector<int> si(26, 0), pi(26, 0);
        int left = 0, right = 0;
        while (right < p.size())
        {
            si[s[right] - 'a']++;
            pi[p[right] - 'a']++;
            right++;
        }
        right--;
        while (right < s.size())
        {
            if (si == pi)
            {
                ans.push_back(left);
            }
            si[s[left] - 'a']--;
            left++;
            right++;
            if (right != s.size())
            {
                si[s[right] - 'a']++;
            }
        }
        return ans;
    }
};